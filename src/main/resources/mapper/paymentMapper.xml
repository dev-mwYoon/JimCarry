<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.app.jimcarry.mapper.PaymentMapper">
    <!-- 추가 -->
    <insert id="insert" useGeneratedKeys="true" keyProperty="paymentId">
        insert into tbl_payment
        (user_id, storage_id, payment_date)
        values(#{user_id}, #{storage_id}, #{payment_date};
    </insert>
    <!-- 전체조회 -->
    <select id="selectAll" resultType="paymentDTO">
        SELECT pay_id, user_id, storage_id, payment_date
        FROM tbl_payment;
    </select>
    <!-- 조회 -->
    <select id="select" resultType="paymentVO">
        select pay_id, user_id, storage_id, payment_date, payment_amount
        from tbl_payment
        where pay_id = #{paymentId}
    </select>
    <!-- 수정 -->
    <update id="update">
        update tbl_payment
        set user_id=#{userId}, storage_id=#{storageId}, payment_date=#{paymentDate}
        where pay_id= #{payId};
    </update>
    <!-- 삭제 -->
    <delete id="delete" parameterType="long">
        delete from tbl_payment
        where pay_id = #{payId}
    </delete>
</mapper>